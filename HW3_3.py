def my_func(num_1: int, num_2: int, num_3: int) -> int:
    m-list = [num_1, num_2, num_3]
    try:
        m_List.remove(min(m_list))
        return sum(m_list)
    exept TypeError:
    return "Enter only a numbers"

